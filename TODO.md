# TODO — Нерешённые проблемы
# qwen --resume a46f5114-dac5-4180-8220-ec5352fc6337

## 1. Бесконечный скролл не работает

### Статус
✅ **ИСПРАВЛЕНО** (2026-04-13)

### Что было сделано
- Убран `fetchInitial()` из обработчика WebSocket `initial` — данные теперь принимаются напрямую из WS сообщения
- Добавлен флаг `loadingInitial` для предотвращения параллельных запросов
- При `initial` сообщении: группы разбиваются на страницы, показывается первая страница
- При `update` сообщении: вызывается `fetchInitial()` (т.к. загружен новый файл)
- Добавлена функция `fetchStoresAndCategories()` для отдельной загрузки списков

---

## 2. Локальный запуск бэкенда

### Статус
✅ **ИСПРАВЛЕНО** (2026-04-13)

### Что было сделано
- Добавлен fallback на SQLite, если `DATABASE_URL` не задан
- `database.py` теперь поддерживает оба режима: PostgreSQL (production) и SQLite (local)
- Создан `backend/.env.example` с примером конфигурации
- SQLite файл (`inventory.db`) автоматически создаётся в `backend/` при первом запуске
- `.gitignore` уже содержит `*.db` и `*.sqlite`, так что локальная база не попадёт в репозиторий

### Как запустить локально
```bash
cd backend
# Без .env файла - будет использоваться SQLite
python main.py
```

---

## Дата создания
2026-04-12

## Дата обновления
2026-04-13
